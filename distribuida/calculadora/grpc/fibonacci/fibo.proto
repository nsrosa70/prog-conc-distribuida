syntax = "proto3";

package fibonacci;

service Fibonacci {
  rpc Fibo (Request) returns (Reply) {}
}

// Mensagem de Request
message Request {
  int32 P1 = 1;
}

//Mensagem de resposta
message Reply {
  int32 N = 1;
}

